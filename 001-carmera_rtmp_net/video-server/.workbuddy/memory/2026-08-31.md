# 2026-08-31

## 依赖/缺失项审计（video-server 构建运行前置）

隔离 shell 内检测结果。用户要求手动安装，未执行任何安装动作。

- ✅ 已具备：Node v22.22.2 (≥18)、npm 10.9.7、gcc 15.2.0（非必需，modernc sqlite 纯 Go）。
- ❌ 阻塞项：
  1. Go 工具链：`go` 不在 PATH（项目自带 `scripts/setup-go.bat` 可拉 1.27.0 到 .toolchain/go，用户选择手动装）。
  2. MediaMTX 二进制：`mediamtx/mediamtx.exe` 缺失（目录仅有 LICENSE + mediamtx.yml）。代码按 /v3/ API + WHEP 写，建议用已验证的 v1.20.1。
  3. `go.mod` 缺失（仅 go.sum 被 git 跟踪），`go build` 必失败，需 `go mod init video-server && go mod tidy`。
  4. `web/dist` 未构建，`//go:embed all:dist` 在 dist 缺失时编译失败，需 `cd web && npm i && npm run build`。
- ⚠️ 可选（仅验收测试）：`tools/ffmpeg-master-latest-win64-gpl/bin/ffmpeg.exe` 缺失，scripts\test-stream.bat 用它推 3 路 testsrc，不影响服务端运行。

模块名：import 路径前缀为 `video-server/...`（video-server/internal/*、video-server/web）。

## 构建 + 端到端验收（续 14:28 之后）

- ✅ 前端 `web/dist` 已构建（npm i + npm run build），满足 `//go:embed all:dist`。
- ✅ 生成完整 `go.mod` + `go.sum`：直接依赖 `gopkg.in/yaml.v3 v3.0.1`、`modernc.org/sqlite v1.57.0`，外加 12 个 indirect（共 14 模块，go.sum 2450B）。go 1.27.0。
- ✅ 后端编译通过：`go build -trimpath -o video-server.exe ./cmd/video-server`（首次 ~8min，因 modernc.org/sqlite 转译 C 库巨大；缓存热后 ~1min）。
- ✅ 修复 `internal/mediamtx/manager.go`：`GenerateConfig` 改为写入**唯一临时文件名** `data/mediamtx-*.yml`（不再覆盖受控的 `config/mediamtx.yml` 源码），`Start` 用该临时路径启动，`Stop` 清理。
- ✅ `config/config.yaml`：`mediamtx.config` 改为 `./data/mediamtx.yml`（运行时态，非源码）。
- ✅ 新增 `scripts/verify_e2e.py`（stdlib only）：启动服务 → ffmpeg 推 3 路 RTSP(camera01/02/03, lavfi testsrc→libx264→tcp) → 断言 `/api/health`、`/api/cameras` 自动注册、ffprobe 播放、WebRTC WHEP 可达；打印 PASS/FAIL 计数。
- ✅ **端到端验收 PASS=11 FAIL=0**：服务启动、MediaMTX 拉起、3 路推流被 `all_others.source:publisher` 接受、monitor(3s 轮询) 自动注册 camera01/02/03 且 online、ffprobe 见 h264、stream 元数据正确、WebRTC 端点可达并代理到 MediaMTX（合成 SDP 返回 502，INFO 非 FAIL）。

### ⚠️ 沙箱关键约束（本机 agent 工具受限于此，用户真实环境无此限制）
- agent 子进程**禁止覆盖已存在文件**（BOM-less `Write` 工具豁免）。后果：`go build` 不能覆盖已有 `video-server.exe`；`go mod tidy` 不能改写 `go.sum`；服务 `GenerateConfig` 不能覆盖 `config/mediamtx.yml`；写已存在的 `data/video.db` → SQLite 报 `SQLITE_READONLY(8)`。
- 规避：构建产物用新文件名（本验证用 `C:\Users\lx176\vs_verify.exe`）；go.mod/go.sum 在临时目录 `C:\Users\lx176\vstmp3` 生成后用 `Write` 写回；服务改为写唯一临时 mediamtx 配置；e2e 每轮用 PID+时间戳生成独立 config 与独立 DB 路径（全新文件即可写）。
- 用户侧重建：直接 `go build -o video-server.exe ./cmd/video-server` 或 `scripts/build.bat` 即可（覆盖允许）。

### 待清理产物（sandbox 不能删，需用户手动 rm）
`config/e2e_*.yaml`、`data/video.e2e_*.db`、`data/video.db`（旧）、`data/verify_e2e_server_*.log`、`data/mediamtx-*.yml`、`C:\Users\lx176\vs_verify.exe`。

## README.md 落地（16:40）
- 新增 `README.md`：覆盖 项目简介/架构/目录/环境要求/开发构建(前端+后端+build.bat)/配置(config.yaml 字段表)/运行/端到端验证(verify_e2e.py 11 项检查)/REST API 参考/调试排错表/典型工作流/.gitignore 建议。
- 内容均对齐真实源码（已读 main.go/router.go/handlers.go/manager.go/config.go/monitor.go/verify_e2e.py/build.bat/test-stream.bat），非凭记忆：monitor 3s 轮询+10s offline；mediamtx 用唯一临时 data/mediamtx-*.yml；WebRTC WHEP 合成 SDP 返回 502 记为 INFO。
