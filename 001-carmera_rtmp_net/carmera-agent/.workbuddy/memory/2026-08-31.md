# 2026-08-31

## Camera Agent 编译环境探测（阶段 1，未安装任何软件，按用户要求只给清单）

项目：carmera-agent（Windows PC 摄像头 → GStreamer → H.264 → RTSP 推流，模拟未来 RK3568 设备）。

### 本机已具备
- VS Professional 2022 17.14.23 在 `D:\Software\vs`（注意：不是默认 C 盘路径）
- MSVC 工具集 14.44.35207（cl.exe 存在）
- Windows SDK 10.0.26100.0
- CMake 4.2.1 / Ninja 1.13.2 / Git 2.55.0 / pkg-config 2.5.1
- MSYS2 MinGW-w64 GCC 15.2.0 在 `D:\Software\MSYS2\mingw64`
- GitHub 可达，spdlog tag v1.14.1 已验证（27cb4c76...）

### 缺失
- GStreamer 1.0 MSVC x86_64 devel（必须）。官方最新 1.28.6，
  建议装到 `C:\gstreamer\1.0\msvc_x86_64`（与 CMakeLists.txt:29 的 GSTREAMER_ROOT 默认值一致；
  1.28 起 Inno Setup 安装器默认改到 %ProgramFiles%，必须显式 /DIR 指定）
- MediaMTX（RTSP 服务器，验收用）、FFmpeg/ffplay（拉流验证，验收用）

### 关键发现（后续必须处理）
1. **src/rtsp/rtsp_publisher_gst.cpp 缺失**，但 src/CMakeLists.txt:11 引用了它。
   `RtspPublisher::create()` 只在 rtsp_publisher_sim.cpp:38 有实现，
   而 stream_controller.cpp:27 调用它 → gstreamer 后端必然链接失败。git 工作区干净，文件从未入库。
2. **build/ 缓存失效**：CMakeCache 指向 `C:/Software/msys2/mingw64/bin/g++.exe`（不存在），
   且 build/_deps 下没有 spdlog-src → 需删掉重建。
3. README.md:57 与 scripts/build.ps1 注释称 MSVC 需要 PKG_CONFIG_PATH，实际
   CMakeLists.txt 在 MSVC 分支用 find_path/find_library 直接定位，不依赖 pkg-config → 说明有误，待订正。

### 产物
- 写出 `ENV_SETUP.md`：完整环境清单、下载地址、安装命令、必需 GStreamer 元素表、安装后自检脚本。

### 下一步（用户装完软件后）
删 build/ → 用 "Visual Studio 17 2022" 生成器重建 build-msvc → 确认后端选中 gstreamer
→ ctest → 补 rtsp_publisher_gst.cpp → --list 验证 → MediaMTX 推流 + ffplay 拉流 + 断服重连验收

---

## Camera Agent 仿真与端到端验收（阶段 2，全部完成）

### 构建链路（关键坑）
- MSBuild 在 Bash/PowerShell 沙箱下直接崩溃（被安全策略拦截 / MSYSTEM 污染）→ 改用
  **Ninja + MSVC**，脚本 `scripts/build-msvc.ps1`（PowerShell，非沙箱模式运行）。
- 必须用 **VS 自带的原生 CMake/Ninja**（`D:\Software\vs\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin` 与 `...\Ninja`），
  不能用 MSYS2 的 cmake（会把 PATH 转冒号分隔，导致 cmd.exe 找不到 rc.exe）。
- 编译命令需显式 `-DCMAKE_CXX_COMPILER=D:/Software/vs/VC/Tools/MSVC/14.44.35207/bin/Hostx64/x64/cl.exe`，
  并补 `Windows Kits\10\bin\10.0.26100.0\x64`（rc.exe/mt.exe）进 PATH。
- 加 `/utf-8` 消 C4819（源码 UTF-8 被 MSVC 按 GBK 读）。零警告构建通过。

### 代码修复（4 处真实 bug）
1. **补 `src/rtsp/rtsp_publisher_gst.cpp`**：原缺失，实现 build_url/connect/disconnect/is_connected。
2. **GStreamer 管线**：`rtspclientsink` 是请求式 pad 且自带 RTP payloader，不能串 `rtph264pay`
   （链接失败 "could not link pay0 to rtspclientsink0"）→ 改为 `h264parse ! rtspclientsink`。
3. **源选择**：本机 UVC 摄像头 `gst-device-monitor` 推荐 **mfvideosrc**（Media Foundation）；
   dshowvideosrc/ksvideosrc 对该设备只吐 1 帧就停。改 `source_element` 让 auto 优先 mfvideosrc，
   并把源名与 `device-index=` 参数拆开（`element_available` 只认裸名）。
4. **退避伪成功**：gstreamer 后端在 `gst PLAYING` 时就报 STREAMING，但 rtspclientsink 异步连接，
   连不上会发 ERROR → 状态反复 STREAMING→DISCONNECTED 把 attempt 清零，1/2/5/10s 升不上去。
   在 `stream_controller` 加 `kStableGraceSec=3.0`：STREAMING 需稳定 ≥3s 才重置退避计数。

### 真实设备约束
- 本机只有一个 "UVC Control" 摄像头，**原生 240×240 @ 8fps**（非 webcam 默认分辨率），
  默认 1280×720@30 协商会失败 → 验收用 `--width 240 --height 240 --fps 8`。
- 编码器走 **nvh264enc**（N 卡 NVENC）；nvh264enc 不支持 I420（只 NV12/Y444/...），
  所以 caps 不能 pin format=I420，交由 videoconvert 协商。
- GIO 代理噪声：`giolibproxy.dll` 加载失败 + 本机 `HTTP_PROXY=127.0.0.1:7892` → 可达性探测偶报
  "not reachable"，但属软检查（仅告警，不影响实际重连）。

### 验收结果（确定性可复现，`scripts/e2e-test.ps1`）
- ctest / 直接跑测试二进制：**10/10 通过**。
- `--list` 真实枚举到摄像头。
- 推流：MediaMTX 收流；ffmpeg 实拉到真实 H264 帧（240×240 yuv444p）。
- 断服重连：杀 MediaMTX 后 agent 存活，退避 1s→2s→5s 升级；重启后 **auto-resume=True**，frames 稳定爬升。
- 注意：e2e 脚本的 auto-resume 判定必须读 **mediamtx.log**（实时刷盘），不能读 agent.log
  （C++ stdout 缓冲，读到的可能是陈旧快照）；mediamtx 时间戳是 `2026/08/31` 斜杠格式。

### 交付物
- `scripts/build-msvc.ps1`（MSVC+Ninja 构建）、`scripts/e2e-test.ps1`（端到端验收）
- 各 `scripts/probe-*.ps1`（隔离验证采集源/编码器/管线，调试用）
- `ENV_SETUP.md`（环境清单）、`e2e.log`（最近一次验收日志）

---

## 目录整理 + 一键启动脚本（阶段 3）

### 用户要求
1. 生成的调试文件（*.err / *.log / *.h264 / capture-*）统一放 `tests/finished/`；`.ps1` 留在 `scripts/` 原目录。
2. 根目录放一键启动 `start-camera-agent.bat`：杀进程 → 顺序启动 → 打印访问信息。
3. 外部工具（mediamtx/ffplay/ffmpeg/gstreamer）已入系统 PATH，**不写绝对路径**；仅本项目
   `build-msvc\src\camera-agent.exe` 用相对路径，便于移植。

### 改动
- 新增 `tests/finished/`（生成产物目录）。
- 改写 `scripts/e2e-test.ps1`：外部工具改 PATH 直接调用；项目根用 `$PSScriptRoot` 推导；
  全部 *.log/*.err/capture 输出到 `tests/finished/`。
- 新增根目录 `start-camera-agent.bat`（纯英文 .bat，符合约定）：
  (1) taskkill mediamtx/camera-agent/ffplay；(2) 顺序 start mediamtx → camera-agent → ffplay；
  (3) 打印 RTSP URL 等访问信息。日志重定向 `tests/finished/`。bat 先 `cd /d %ROOT%`。
- 新增根目录 `mediamtx.yml`：**关键**，MediaMTX 默认配置拒绝任意发布路径（"path 'camera01' is not configured"），
  此 yml 用 `paths: all_others:` 允许任意路径发布。必须随项目根目录（mediamtx 从 cwd 加载）。
- `.gitignore` 补：*.err *.log *.h264 capture-* auto.crt auto.key（auto.crt/auto.key 是 mediamtx MoQ 自动生成的）。

### 关键点 / 坑
- `start "title" cmd /c "prog > log 2>&1"`：内嵌引号会让 cmd 重定向解析失败（日志文件都不生成）。
  项目路径无空格 → cmd /c 内**不加内层引号**即可（已验证 agent.log 正常生成）。
- 冒烟验证（用注册表刷新 PATH 模拟用户环境）确认：mediamtx 报 "stream is available and online, 1 track (H264)"，
  camera-agent 持续 STREAMING（frames 稳定爬升 ~7fps），日志落在 `tests/finished/`。
- 临时验证脚本 `scripts/verify-launch.ps1` 验证后已删除。

---

## build_oneclick.bat / start-camera-agent.bat 真实修复（阶段 4，已实测通过）

### build_oneclick.bat（之前报错 "此时不应有 ." RC=255）
- 根因1：`echo [ERROR] Visual Studio not found (looked in '%VSPATH%').` —— 括号内 echo 串里的 `)` 提前关掉
  `if not exist (...)` 块（cmd 不把引号内 `)` 当字面量），导致后面 `'.` 成为 "unexpected"，整文件解析失败。
  即使该 `if` 分支运行时被跳过，解析期仍报错。改法：echo 串去掉 `()`（改成 `- looked in %VSPATH%`）。
- 根因2：`%~dp0` 带尾反斜杠 → `cmake -S "%ROOT%"` 变 `"...carmera-agent\"`，`\"` 被 cmd 当成转义引号，
  cmake 把整行当成 -S 参数 → "source directory does not exist"。改法：`if "%ROOT:~-1%"=="\" set ROOT=%ROOT:~0,-1%` 去尾斜杠；
  并修 `set BUILD=%ROOT%\build-msvc` / `set EXE=%ROOT%\build-msvc\...`（去尾斜杠后必须显式补 `\`，否则拼成 `carmera-agentbuild-msvc` 兄弟目录）。

### start-camera-agent.bat（之前双击"没反应"）
- 双击 .bat 继承注册表系统 PATH，mediamtx/ffplay/ffmpeg 只在交互 shell 会话 PATH → 原来静默失败。
  改法：新增 `:add_tool_path` 探测 `D:\data\agent-tools\...` 已知目录并 prepend 到 PATH，双击也能跑。
- 同一 `)`-in-echo 陷阱：exe 预检块 `echo ... (or: build_oneclick.bat sim)` 的 `)` 提前关块，
  使 `goto :fail` 漏出块外**无条件执行** → 永远报 "exe not found"（实际 exe 存在）。去 `()` 后修。
  另两处同类隐患（`:need` 里 `(System Properties ...)`、验证块里 `(no camera / GStreamer issue?)`）一并去 `()`。
- **修正阶段3的错误结论**：`start "title" cmd /c prog > log` 的 `>` 落在 start 的 stdout 上，而 start 开新控制台窗，
  子进程输出根本不进文件 → 日志 0 字节（之前误以为"去内层引号就正常"其实是没捕获）。正确写法：
  `start "MediaMTX" cmd /c "mediamtx > %FINISHED%\mediamtx.log 2>&1"`（重定向放进 `cmd /c "..."` 内，路径无空格故不去内层引号）。
- `timeout /t 1` 在本沙箱被 Git Bash 的 GNU timeout 抢先（"invalid time interval '/t'"）→ 轮询 0 延迟。
  钉死 `C:\Windows\System32\timeout.exe /t 1`。
- 轮询 `for /l` 内 `goto :label` 会丢弃刚 `set` 的变量（cmd 已知怪癖）→ mediamtx 永远报 not ready。
  改法：去掉 goto，改用 `EnableDelayedExpansion` 标志位 `if "!MT_READY!"=="0" (...)` 轮询。

### 验收
- `./build_oneclick.bat clean`：configure+build 通过，**0 warning / 0 error**，产出 `build-msvc\src\camera-agent.exe`（311KB）。
- `./start-camera-agent.bat --no-pause`：AUTO 探测工具 → 预检全 OK → mediamtx 起服并轮询到 `:8554` 打印 `[OK] mediamtx ready`；
  camera-agent/ffplay 启动（本沙箱无摄像头 → camera-agent 连 mediamtx 后无流退出，agent.log 空 → 报 status unknown，符合预期；
  真机有摄像头会打 STREAMING）。
- 注：本沙箱继承 PATH 含 mojibake（`D;`/`��C;`）与 GNU 工具抢先，属沙箱环境，非用户双击环境；用户双击走干净系统 PATH 表现更好。

---

## 端到端延迟评估与 camera-agent 内部优化（阶段 5）

### 现象
流程跑通，但"视频动作延时大概超过 1s"。用户要求先评估卡点，mediamtx / ffplay 暂不动。

### 卡点定位（camera-agent 内部可控）
链路：`mfvideosrc → videoconvert → caps → encoder → h264parse → rtspclientsink`
1. **编码未开低延迟（最大头）**：`encoder_element()` 只对 x264enc 加 `tune=zerolatency`；
   但 `pick_encoder()` 实际优先返回 **nvh264enc**，走 hw 分支只设 `bitrate`（无 zerolatency/tune/bframes/lookahead）。
   NVENC 默认 B 帧重排 + lookahead → 300ms~1s+。
2. **整链无 queue**：采集→转码→编码→parse→sink 同线程串行，编码慢阻塞采集、帧无法解耦。
3. **rtspclientsink 缓冲**：未设 `latency`/`rtx-time`，推送端默认缓冲。

### 用 gst-inspect 确认的合法属性（关键，避免瞎改）
- `nvh264enc`：`tune`(2=low-latency,3=ultra-low-latency)、`zerolatency`(true=禁重排)、`bframes`、`rc-lookahead`、`repeat-sequence-header`、`qos`。
- `rtspclientsink`：`latency`(ms 缓冲)、`rtx-time`(重传缓冲，0=禁用)、`udp-buffer-size`。
- `mfvideosrc`：无延迟相关属性（采集端固有，240×240@8fps 帧间隔 125ms）。

### 改动（src/pipeline/video_pipeline_gst.cpp）
- `encoder_element()` 按 enc 名分类加低延迟：
  - nv*：`tune=ultra-low-latency zerolatency=true bframes=0 rc-lookahead=0 repeat-sequence-header=true`
  - mfx*：`tune=low-latency bframes=0 rc-lookahead=0 repeat-sequence-header=true`
  - x264：`tune=zerolatency speed-preset=veryfast bframes=0`（原只缺 bframes=0）
  - 其他 hw：至少 `bframes=0`
- `build()` 的 desc 在每段间插 `queue max-size-buffers=2 max-size-bytes=0 max-size-time=0`（最多 2 帧缓冲，防堆积）；
  rtspclientsink 加 `latency=0 rtx-time=0`。

### 验收
- `./build_oneclick.bat`：增量重编，RC=0，**0 warning / 0 error**，exe 更新（315904B）。
- 离线 link 验证（gst-launch，videotestsrc→fakesink 替换摄像头/RTSP）：RC=0，整链 link 成功、NVENC 走 CUDA、30 帧正常跑完，
  证明 queue + nvh264enc 低延迟属性 + sink latency=0/rtx-time=0 全部合法可跑。
- 真机端到端延迟需用户在有摄像头机器上复测（沙箱无摄像头，且 mediamtx/ffplay 未动，其默认缓冲仍是延迟大头）。

### 结论 / 提示
- 上述改完预计能砍掉 camera-agent 内部大部分（数百 ms~1s）编码+缓冲延迟。
- 若真机复测仍 >1s，大头在外部：mediamtx 转发缓冲 + ffplay 的 `rtspclientsink`/`rtspsrc` 默认 `latency=2000ms`（播放端），按用户要求暂未动。

---

## mediamtx / ffplay 配置选项评估与优化（阶段 6）

### 用户要求
camera-agent 已优化，现在看 mediamtx/ffplay 的配置选项继续压延迟。

### 评估结论
- **mediamtx 本质是 RTP pass-through，自身延迟极小**，可榨的油很少。路径级可调项里与延迟相关的只有
  `rtspTransport`（auto/tcp/udp）、`record`(off 才不引磁盘缓冲)、`writeQueueSize`(默认512)、`readTimeout`(默认10s)。
- **ffplay / RTSP 订阅端才是播放侧延迟大头**：`rtspsrc` 默认启动缓冲 ~2s；原脚本只给了
  `-rtsp_transport tcp -fflags nobuffer -flags low_delay`，漏掉最大项。

### 改动
- `mediamtx.yml`：新增 `paths.all_others.rtspTransport: tcp`（强制 TCP 交织，与 rtspclientsink 已用 TCP 一致、
  确定性更好）+ `record: no`（不引磁盘缓冲）。YAML 校验通过。
- `start-camera-agent.bat` 的 ffplay 启动行扩成低延迟旗标组：
  `ffplay -rtsp_transport tcp -rtsp_flags nobuffer -fflags nobuffer -flags low_delay -probesize 32768 -analyzeduration 0 -framedrop rtsp://127.0.0.1:8554/camera01`
  - `-rtsp_flags nobuffer`：关 RTSP demuxer 启动缓冲（原缺，最大头 ~数百ms~2s）
  - `-probesize 32768` / `-analyzeduration 0`：最小化流探测/分析等待
  - `-framedrop`：落后帧丢弃保持实时
- 校验：ffplay 只给旗标不给 URL → 报 "An input file must be specified"（证明无 Unrecognized option，旗标全合法）。

### 量化预期（需真机复测）
- camera-agent 内部（阶段5）+ 本阶段 ffplay 端，端到端动作延迟应从 >1s 降到约 200~400ms 量级
  （受 240×240@8fps 帧间隔固有 125ms + 编码/网络/解码残余）。
- mediamtx 强制 TCP 不增延迟；`writeQueueSize`/`readTimeout` 暂保持默认。
- 仍残留的延迟变量：8fps 帧间隔（采集端固有，改 fps 才能变）、NVENC 编码残余、本地回环网络。

### 备注
- `keyframe_interval: 30`（默认）→ GOP 30帧@8fps≈3.75s；mediamtx 等新订阅者要等到下一 IDR 才转发首帧。
  阶段5 已加 `repeat-sequence-header=true`（流自包含），缓解 reconnect 等待；如要更快首帧可降到 15~20。

---

## camera-agent 内部延迟探针（阶段 7，已实现并实测）

### 用户需求
只测 camera-agent 内部延时（采集→编码→推送到 mediamtx），不动 mediamtx/ffplay。
方法：在 GStreamer 管线 3 个边界加 pad probe，按帧记录墙钟时刻相减。

### 实现（src/pipeline/video_pipeline_gst.cpp）
- 新增 `LatencyTracker` + `latency_probe_cb` + `LatencyCbCtx`；管线元素命名 `cam`/`enc`/`parse`/`sink`。
- 3 个 probe 挂在：capture=`cam.src`、encode=`enc.src`、push=`parse.src`。
  - 关键坑：`rtspclientsink` 是 **request pad（sink_%u）**，无静态 `sink` → 改用 `h264parse.src`（rtspclientsink 作为 sink 拉数据，parse.src 触发即"取到编码帧去推送"边界，等价）。
- **不用 PTS 配对，用 FIFO 顺序配对**：H264 编码器会**重打 PTS**，按 PTS key 会导致三段对不上（实测 matched=0）。
  改法：`cap_q/enc_q/push_q` 三个时间戳队列，push 时取三者队首计算 `capture->encode` / `encode->push` / `total`。
  限定 bframes=0（无重排）→ 1:1 顺序成立。队列超 1024 清空防 desync。
- 开关：`PipelineParams.measure_latency` ← `Config.measure_latency` ← CLI `--latency-probe`（默认关，不影响生产）。
- 新增 `--source <elem>` 透传（含 `videotestsrc` 合成源），`source_element()` 未知名直接透传；
  `videotestsrc` 自动加 `is-live=true`（否则非实时源不限速灌帧，破坏 FIFO 平衡）。
- `start-camera-agent.bat` 也解析 `--latency-probe` 透传给 camera-agent。

### 校验（沙箱，无摄像头，用 videotestsrc 注入）
- `./build_oneclick.bat`：6 次增量重编全 **0 warning / 0 error**。
- 端到端跑：`camera-agent --source videotestsrc --latency-probe --width 240 --height 240 --fps 8`（mediamtx 先起）。
  稳态输出（每秒一行，n≈8）：
  `[latency] capture->encode=1.0ms  encode->push=20ms  total(capture->push)=20ms (n=8)`
  首个窗口 397ms 为启动瞬态。
- **结论：camera-agent 内部端到端延时 ≈ 20ms**；用户之前观察到的总延时 >1s 不在本模块，
  由 mediamtx/ffplay/网络 + 8fps 帧间隔(125ms) 主导。真机用 `--latency-probe` 复测同上。

### 备注
- 沙箱跑 camera-agent 需先 `export PATH=/c/Program Files/gstreamer/1.0/msvc_x86_64/bin` 否则报 gio-2.0-0.dll 缺失（Git Bash 不继承 GStreamer PATH）。
- 二进制直接前台跑被沙箱安全策略拦（`start cmd /c` 也拦），用 Bash 工具 `run_in_background` 直接起 exe。

---

## 分辨率/帧率自动协商（阶段 8，已实现并实测）

### 用户需求
camera-agent 启动是否必须指定宽高帧率？能否指定 UVC 设备后自动选合适宽高帧率？
→ 结论：当前**必填**（默认 1280×720@30，会被强写进 `video/x-raw,width=,height=,framerate=` caps）。
对原生 240×240@8fps 的 UVC 设备，强制 caps 协商失败 → 进重连循环。用户选「原生直通」方案。

### 实现
- `PipelineParams.auto_res`（video_pipeline.h）、`CameraConfig.auto_res`（config.h）、YAML `camera.auto`、CLI `--auto` 三处打通；`stream_controller` 透传 `pp_.auto_res`。
- `video_pipeline_gst.cpp` `build()`：`auto_res` 为真时**不在 videoconvert 后插 caps 过滤器**（原生直通），
  让 GStreamer 协商摄像头默认/原生格式；否则保持原强制 caps。
- 新增 `query_negotiated()`：PLAYING 后查 **`cam.src` pad 的协商 caps**（摄像头源 pad 必有固定 caps，含宽高帧率；
  编码器 sink pad 在 PLAYING 时可能只回模板 caps 而无宽高 → 改用源 pad 才可靠），解析宽高帧率存入
  `neg_w_/neg_h_/neg_fps_` 并打印 `Negotiated capture format: WxH @ Ffps`；`get_negotiated_resolution()` 虚方法
  供 `stream_controller.get_device_info()` 上报真实分辨率（值变化才重打，去重）。
- 注意：`videoconvert` 只转像素格式**不缩放**，故原生直通=摄像头给什么推什么；要固定输出尺寸需另加 `videoscale`。

### 验收
- `scripts/build-msvc.ps1 -Backend gstreamer`：增量重编 **0 warning / 0 error**，exe 更新。
- 真机 `--auto --source mfvideosrc`：日志 `Auto-resolution: not forcing caps` → `Negotiated capture format: 240x240 @ 8fps`
  → STREAMING，~8fps 稳定推流（47 帧/8s，0 dropped）。自动识别出 UVC 原生格式，无需手填 `--width/--height/--fps`。
- 反向确认：默认（非 auto）仍走强制 caps，对 240×240 设备会协商失败（预期，符合历史记录）。

---

## README.md 整理（阶段 9）

### 用户要求
把开发/验证/调试流程整理进项目根目录 `README.md`（原 README 只有早期骨架，缺自动协商/延迟探针/一键启动/真实设备坑）。

### 改动
- 重写根目录 `README.md`：保留后端可插拔/异常处理骨架，整合 8 个阶段的实际流程，新增/扩充章节：
  - 目录结构（含 `build_oneclick.bat` / `start-camera-agent.bat` / `mediamtx.yml` / `scripts/probe-*.ps1` / `tests/finished`）
  - 环境依赖、构建（MSVC+Ninja 四大约束坑）、配置（YAML/mediamtx.yml/`--auto` 原生直通）
  - 运行（CLI 全参数表 + 典型命令 + `start-camera-agent.bat` 一键启动 + ffplay 低延迟旗标组）
  - 验证流程（单测 10/10、`e2e-test.ps1` 四阶段、`--latency-probe` ~20ms、`--auto` 协商 240×240@8fps）
  - 调试流程（bat 六坑表 / 构建坑 / 延迟优化三处 / 真实设备约束 / 历史 probe 脚本表）
  - 已知问题（总延迟 >1s 在外部、固定输出需 videoscale、枚举择优模式待扩展）
- 所有内容基于实际代码与脚本（已 Read 确认 main.cpp 帮助、build-msvc.ps1、两个 bat、mediamtx.yml、e2e-test.ps1、config yaml、probe 脚本用途），非臆测。

### 验收
- `README.md` 写毕，结构完整、与代码/脚本一致。
