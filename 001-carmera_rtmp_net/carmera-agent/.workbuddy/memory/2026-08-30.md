# 2026-08-30

## Camera Agent (PC 摄像头模拟嵌入式 Linux 摄像头设备) 初始化完成

- 工程位于 `E:\cnb\git\SOC-Agent\001-carmera_rtmp_net\carmera-agent`，技术栈 C++17 + CMake + GStreamer + spdlog。
- 按用户 8 阶段（环境检查/枚举/采集/编码/RTSP/重连/配置/测试）落地，逐阶段编译+运行验证。
- 关键设计：**后端可插拔** `auto/gstreamer/sim`。沙箱无 GStreamer/摄像头，默认 auto→sim；
  SIM 后端用内部伪帧驱动同一套状态机/重连/统计，可在无头环境编译测试。GStreamer 真实端
  （ksvideosrc/dshowvideosrc → videoconvert → x264enc/硬件H264 → h264parse → rtph264pay →
  rtspclientsink）代码已写，仅用户真机（装 GStreamer+MSVC）编译验证。
- 依赖最小化：仅 spdlog (FetchContent)；YAML 用手写极简解析器；测试用内置轻量框架（无 gtest）。
- 验证结果：g++ SIM 后端 `cmake --build` 零警告零错误；`ctest` 10/10 通过（覆盖枚举/管线/编码/
  RTSP连接/RTSP断开/重连/参数错误/退出/退避调度）。`--list`/`--version`/`--config`+CLI 覆盖均验证。
- 断线重连：状态机 + `BackoffScheduler`(1/2/5/10s 封顶)，独立线程驱动，服务器恢复自动重连，不退出。
- 注意：GStreamer 端（`*_gst.cpp`）本沙箱未编译，真机验收需注意 pkg-config 路径与 rtspclientsink（gst-rtsp-server）。
- 身份引导文件 BOOTSTRAP.md 等仍在工作区根 `~/.workbuddy`，本次未处理（用户聚焦工程交付）。
