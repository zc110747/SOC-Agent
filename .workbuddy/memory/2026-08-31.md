# 2026-08-31

## 将 001-carmera_rtmp_net/carmera-agent 提炼为可复用 skill（面向 SOC 项目）

- 新建 `~/.workbuddy/skills/soc-camera-rtsp-agent/SKILL.md`
  - 摄像头 RTSP 推流 Agent 的可插拔后端架构（SIM 仿真 / GStreamer / 未来 V4L2）
  - 断线退避重连（{1,2,5,10}s 封顶 10s + kStableGraceSec=3.0 稳定宽限）
  - 极简无依赖 YAML 配置、零依赖单测框架、MediaMTX 端到端验收范式
- 新建 `~/.workbuddy/skills/soc-windows-gstreamer-build/SKILL.md`
  - Windows MSVC+Ninja 构建 GStreamer C++ 工程踩坑：禁用 MSBuild、用 VS 自带 cmake/ninja、PowerShell 保 PATH、/utf-8 消 C4819、GStreamer MSVC devel 探测（find_path/find_library，非 pkg-config）
- 用户要求：skills 主要复用于 SOC 项目，标题带 `soc-` 前缀
- 追加：用户要求把两份 skill 同时落到工程仓「系统目录」`D:/user_project/git/SOC-Agent/.workbuddy/skills/`（项目级，随仓库版本化/团队共享），用户级 `~/.workbuddy/skills/` 作为全局兜底保留。两处 SKILL.md 内容一致。
